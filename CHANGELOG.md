# Changelog

## v3.3.0 (2026-07-15)

### 改进

- **备忘录加载性能**：自动标签在创建和编辑时直接持久化，本地与外部数据库各执行一次历史数据迁移，列表读取不再额外调用标签识别命令
- **大列表加载**：剪贴板、备忘录及回收站改为每页 50 条增量加载，并补充常用排序条件的本地 SQLite 与外部 PostgreSQL 索引
- **设置写入稳定性**：设置修改按顺序持久化，失败时只回滚当前字段，避免快速切换多个开关时覆盖其他已修改状态
- **更新检查可靠性**：更新检查拆分为独立模块，使用标准语义化版本比较，增加请求超时和 HTTP 状态校验，并优先返回 Windows 安装器直链
- **菜单事件复用**：设置、存储和实验功能菜单统一使用外部点击 Hook，减少重复的全局事件监听代码
- **连接列表一致性**：已保存数据库连接仅在写入成功后更新界面状态，避免持久化失败时界面与配置不一致
- **语言切换稳定性**：国际化上下文独立为稳定模块，语言写入按顺序执行并在失败时回滚，修复切换外部数据库后界面变英文且语言按钮失效的问题
- **存储切换刷新**：本地与外部存储切换只触发一次数据刷新，避免远端图片较多时重复请求持续覆盖加载状态；休眠唤醒仍保留分阶段重试
- **远端图片按需加载**：外部存储列表不再一次传输全部图片 Base64，条目先显示并在图片接近可视区时按需加载，避免大数据量下计数已更新但列表因超时显示为空
- **存储失败回滚**：保存配置或切换已保存连接失败时同步恢复表单、连接指示和已生效配置，并阻止重复提交
- **备份界面整理**：移除“备份/恢复”的测试版标签，继续保留跨版本恢复兼容性提示
- **菜单样式边界**：经典 UI 的设置类菜单恢复实体背景；新版 UI 将菜单移出标题栏合成层，并增加 WebView 磨砂回退底色，修复正式构建中毛玻璃失效和经典界面被连带改动的问题

### 技术细节

- 新增 Vitest 与 Testing Library 前端测试基线，覆盖设置 Schema 加载、设置写入队列、语言切换失败回滚和菜单外部点击行为
- 新增更新检查、备忘录标签推断和标签迁移测试，覆盖 `3.10.0 > 3.9.0`、安装器选择与历史数据回填
- 后端更新检查拆分到 `update.rs`，剪贴板与备忘录命令拆分到 `commands/`；备忘录分页和拖拽交互分别收拢到独立 Hook
- 应用、Tauri 配置、Rust 包、双语文档和官网软件版本展示同步到 `3.3.0`

## v3.2.0 (2026-07-13)

### 改进

- **远程存储流畅性**：外部 PostgreSQL 改用最多 4 个连接的受控连接池，远程查询、搜索、计数和写入统一在后台阻塞任务中执行，避免网络 I/O 占用界面命令线程
- **设置加载性能**：应用设置和远程连接配置支持批量读取与事务写入，减少启动和打开设置面板时的重复 IPC 与数据库锁竞争
- **多设备实时刷新**：客户端接入 PostgreSQL `LISTEN / NOTIFY`，远端写入会通知其他设备；前端合并短时间内的通知后刷新当前列表与计数，并在断线后自动重连
- **并发编辑保护**：剪贴板条目和备忘录使用远端 `version` 乐观锁，其他设备已修改的内容不会被静默覆盖，界面会刷新最新内容并提示重新编辑

### 技术细节

- **设置状态统一**：新增设置 Schema、`useAppSettings`、共享 Provider 和通用设置行组件，移除 `App`、设置菜单和实验功能菜单之间的重复状态与开关代码
- **连接生命周期**：切换外部数据库配置时自动失效旧连接池；连接池限制并发连接数，并设置连接检测、空闲回收和最大生命周期
- **迁移管理**：远端 schema 使用 `schema_migrations` 记录版本，并用 PostgreSQL advisory lock 串行化初始化和后续迁移
- **存储边界**：本地 / 外部存储分发收拢到 `storage_backend`，已保存连接资料拆分为独立前端模块，减少命令层和设置面板的重复逻辑

## v3.1.0 (2026-07-10)

### 新功能

- **多数据库连接管理**：成功连接并切换过的外部 PostgreSQL 数据库会保存为本机连接配置，最多保留 12 条
- **快速切换数据库**：存储设置中新增“已保存连接”列表，可直接选择配置或重新测试后切换到对应数据库
- **连接删除确认**：删除已保存连接前使用软件统一样式弹窗确认，删除操作不会影响外部数据库及其中的数据
- **多色模式(Tab标签)（实验功能）**：可选地让文本、链接、图片、代码、邮箱和路径 Tab 的选中背景显示各自分类色，默认关闭

### 改进

- **连接记录去重**：按连接方式、地址、数据库、账号和 SSL 配置识别相同连接，重复使用时更新时间而不是生成重复记录
- **切换失败保护**：切换已保存连接失败时恢复此前有效配置，避免当前存储状态被无效连接覆盖
- **双语界面补全**：已保存连接、使用、删除和最后使用时间等字段支持中文与英文
- **编辑与原文复制**：点击条目复制最新编辑结果；展开后点击原文内容可复制首次捕获的原始内容，二者互不覆盖
- **新版 UI 交互**：分类 Tab 的选中底色改为平滑滑动；设置类菜单和开关切换减少视觉闪烁
- **界面细节修复**：修复旧版备忘录搜索框聚焦时出现深色底边的问题，并整理新版备忘录新建按钮样式

### 技术细节

- **版本同步**：应用、Tauri 配置、Rust 包、双语文档和官网软件版本展示同步到 `3.1.0`
- **质量验证**：前端构建、ESLint、Rust 格式检查、单元测试和 Clippy 检查均通过

## v3.0.0 (2026-07-09)

### 新功能

- **新版界面升级**：新版 UI 加入毛玻璃标题区、Tab 栏、列表条目、底栏、设置菜单和确认弹窗视觉
- **备忘录视觉标识**：新版 UI 下备忘录条目新增左侧配色边框，继续使用备忘录独立配色
- **实验功能开关**：实验功能新增“隐藏条目色条”，可隐藏剪贴板条目左侧色条和原文左侧边框

### 改进

- **Tab 栏体验**：优化新版 UI 的 Tab 半透明质感、间距、选中阴影和滚动层级
- **设置菜单质感**：设置、存储设置和实验功能菜单统一为毛玻璃浮层，并增强 WebView 兼容写法
- **预览背景稳定**：原格式预览和原文区域改用独立中性背景，不再被樱花粉等主题色染色
- **条目展示优化**：新版 UI 的剪贴板和备忘录条目改为更清晰的卡片行，并减少悬浮时的强边框干扰

### 技术细节

- **冗余清理**：忽略并清理开发运行日志目录 `.codex-run/` 和旧 `tauri-dev.log`
- **版本同步**：应用、Tauri 配置、Rust 包、文档和官网展示同步到 `3.0.0`

## v2.5.0 (2026-07-09)

### 新功能

- **实验功能入口**：设置中新增“实验功能”开关，开启后标题栏显示实验功能按钮
- **实验功能选项**：“新版界面”和“剪贴板多标签显示”迁入实验功能，实验功能及内部选项默认关闭

### 改进

- **字段对齐**：统一备忘录底部时间、编辑时间、标签、“显示更多”、剪贴板标签、分类 Tab 和新建备忘录按钮的垂直居中
- **备忘录布局**：“显示更多”不再使用大面积自动间距，避免把标签挤到中间
- **存储设置正式化**：存储设置入口默认显示，外部 PostgreSQL 存储不再标记为测试版
- **图标统一**：引入 `lucide-react`，统一主要操作按钮图标
- **备忘录标签识别**：备忘录自动标签复用剪贴板分类器，并过滤旧的错误自动标签显示

### 技术细节

- **官网同步**：官网内容同步为软件 `2.5.0`，官网独立版本号升至 `1.5`
- **版本同步**：应用、Tauri 配置、Rust 包、文档和官网版本同步为 `2.5.0`

## v2.3.2 (2026-07-08)

### 改进

- **窗口拖动优化**：窗口拖动期间临时关闭界面过渡动画，并为列表条目增加渲染隔离，减少拖拽时 WebView 重绘卡顿
- **恢复刷新优化**：取消普通窗口聚焦触发数据刷新，改为休眠/网络恢复检测，避免点住标题栏拖动时触发列表刷新
- **标签排序**：剪贴板分类标签支持直接拖拽排序，新增“标签排序”开关，排序结果会保存到设置中
- **存储设置样式**：存储设置标题右侧显示“本地存储 / 外部存储”状态，移除重复的本地/外部模式短标

### 技术细节

- **版本同步**：应用、Tauri 配置、Rust 包、文档和官网版本同步为 `2.3.2`

## v2.3.0 (2026-07-08)

### 修复

- **更新检查修复**：修正版本比较逻辑，将单数字 minor 版本按发布习惯归一化，避免版本线再次回退
- **备忘录编辑时间**：备忘录编辑保存后立即刷新本地 `updated_at`，退出编辑态后马上显示“编辑于刚刚”

### 改进

- **备份格式升级**：本地备份/恢复改为新版 `.scbackup` 包格式，内含清单、数据和校验信息
- **备份兼容提醒**：备份/恢复标题标记为测试版，并显示备份创建版本、当前恢复版本与跨版本恢复风险
- **外部模式保护**：存储设置切换到外部模式时隐藏备份/恢复区域，避免误以为已备份外部 PostgreSQL 数据
- **外部状态可见**：存储设置面板增加本地 / 外部连接状态，保存切换后立即刷新显示
- **搜索体验优化**：剪贴板、备忘录和回收站搜索改为多关键词匹配，减少整句搜索不命中的情况
- **备份按钮状态**：创建备份和恢复备份拆分进行中状态，避免两个按钮互相显示错误的操作文案

### 技术细节

- **旧格式收敛**：新版不再读取旧 gzip `.scbackup` 和裸 `.json` 备份，降低恢复链路复杂度
- **刷新事件收敛**：窗口唤醒、剪贴板变化和存储模式切换统一走前端事件总线触发刷新
- **存储状态封装**：新增存储状态查询模块，避免设置面板直接拼接后端健康状态逻辑
- **版本同步**：应用、Tauri 配置、Rust 包、文档和官网版本同步为 `2.3.0`

## v2.2.0 (2026-07-08)

### 新功能

- **存储设置(测试版)**：系统设置中新增开关，开启后才在设置按钮旁显示存储配置入口
- **外部存储模式**：存储设置支持本地 / 外部 PostgreSQL 两种模式，外部模式保存时自动测试连接并初始化远端表
- **备份/恢复归位**：原本隐藏的数据导出备份能力移动到存储设置中，和存储模式统一管理

### 改进

- **存储文案整理**：存储配置入口标题改为“存储设置”，中文模式名调整为“本地 / 外部”，外部模式说明去除重复描述
- **连接表单调整**：手动填写连接信息时，账号输入框显示在数据库输入框前
- **远程模式流畅性**：列表读取复用连接配置并减少标签切换、搜索和统计刷新时的阻塞感
- **外部模式恢复刷新**：电脑从休眠或网络恢复后自动刷新列表和标签数量，避免需要手动切换标签才恢复显示
- **官网 SEO**：官网加入 Google Search Console 验证标记，并移除未支持平台说明

### 技术细节

- **非商用许可**：新增 `LICENSE.md`，文档明确未经许可不允许商业用途
- **版本同步**：应用、Tauri 配置和 Rust 包版本同步为 `2.2.0`

## v2.1.6 (2026-07-06)

### 新功能

- **更新体验增强**：检查更新结果显示当前版本、最新版本、Release 名称和更新日志摘要，并保留下载入口
- **官网**：新增 `docs/` 静态官网，支持中文/英文切换并可由 GitHub Pages 发布
- **官网版本号**：官网使用独立版本号 `1.0.1`，与软件版本号分开维护
- **官网系统支持**：官网新增系统支持区，标注 Windows x64 当前支持

### 改进

- **启动显示稳定性**：窗口启动时先定位再显示，避免左侧短暂白窗闪烁
- **托盘提示**：托盘图标悬浮提示显示当前语言下的软件名
- **快捷键唤起逻辑**：窗口被其他应用遮挡时按快捷键会重新拉到前台，只有已在前台时才隐藏
- **官网可访问性与 SEO**：增加键盘跳转入口、清晰焦点态、语言按钮状态、搜索关键词、社交分享信息和结构化数据
### 技术细节

- 备份/恢复代码暂时保留为隐藏能力，入口不在设置面板展示
- **版本同步**：应用、Tauri 配置和 Rust 包版本同步为 `2.1.6`

## v2.1.0 (2026-07-03)

### 修复

- **图片剪贴板去重**：图片历史现在使用真实像素数据参与 SHA-256 去重，不再只按尺寸和字节长度判断，避免同尺寸图片被误判为重复
- **搜索请求状态**：修复搜索防抖后可能用旧关键词查询、并触发重复请求的问题
- **查询边界**：剪贴板和备忘录查询的 `limit/offset` 增加范围修正并改为参数绑定，避免异常分页参数拖慢查询
- **回收站备忘录主题色**：回收站中的备忘录标题、正文和时间颜色改为主题变量，深色模式下保持一致
- **备忘录标签配色**：用户手动添加的备忘录标签跟随备忘录配色；自动识别标签继续沿用剪贴板分类颜色

### 改进

- **自动标签识别集中**：备忘录自动标签类型由后端分类器统一判断，前端只负责翻译和展示，减少剪贴板分类与备忘录标签逻辑漂移
- **组件拆分**：拆出回收站备忘录条目和确认弹窗组件，降低 `App.tsx` 复杂度
- **存储版本标记**：数据库初始化写入 `schema_version`，为后续显式迁移打基础
- **版本同步**：应用、Tauri 配置和 Rust 包版本同步为 `2.1.0`

## v2.0.2 (2026-07-01)

### 修复

- **检查更新结果判断**：修复后端返回 `has_update/latest_version/download_url`，前端读取 `hasUpdate/latestVersion/downloadUrl` 导致检查更新始终显示“已是最新版本”的问题
- **更新接口兼容性**：后端更新信息统一输出 camelCase，前端同时兼容 camelCase 与 snake_case 字段，避免后续字段名不一致再次误判

## v2.0.1 (2026-07-01)

### 变更

- **启动显示窗口**：软件启动后直接显示桌面主窗口，不再只在后台托盘常驻
- **多进程托盘修复**：包含单实例启动修复，反复点击桌面快捷图标或重复启动程序时会唤起已有窗口，不再创建多个进程和重复托盘图标
- **点击即粘贴现状记录**：文档补充当前自动粘贴依赖焦点回切和延迟发送 Ctrl+V，部分应用或慢焦点切换场景下可能不够灵敏，暂不在本版本重构
- **版本同步**：应用、Tauri 配置和 Rust 包版本同步为 `2.0.1`

## v2.0.0 (2026-07-01)

### 新功能

- **托盘设置重置位置**：右键托盘图标选择「设置」打开应用时，窗口位置自动重置为右半屏居中默认位置
- **应用正式命名**：软件名和项目名统一为 `SuperClipboard`，中文名为「超级剪贴板」
- **仓库名同步**：GitHub 仓库名同步为 `SuperClipboard`
- **首次语言跟随系统**：首次启动根据系统语言自动选择中文或英文，托盘菜单同步初始化
- **备忘录图片粘贴**：备忘录编辑区支持 Ctrl+V 粘贴图片，预览区自动渲染为图片，不再把 base64 文本暴露给用户
- **存储分开显示**：底栏存储按页面分别显示——备忘录页显示备忘录内容占用，剪贴板页显示剪贴板内容占用，SQL 排除已归档条目
- **默认初始状态同步**：默认快捷键为 `Alt+X`，主题模式跟随系统，主题配色为少年蓝，开机自启和自动检查更新默认开启，窗口置顶、原格式预览、备忘录和回收站默认关闭

### 改进

- **归档→回收站**：「归档模式」重命名为「回收站」，更符合用户直觉。归档按钮图标从文件夹（📁/📦）统一更换为废纸篓（🗑️），包括剪贴板条目和备忘录的删除按钮
- **底栏高度统一**：修复剪贴板页底栏因清除按钮 padding 导致的高度不一致，设置 min-height 并缩减按钮内边距
- **默认位置居中**：默认窗口位置从贴近右边缘改为右半屏水平居中（`(left + right*3)/4 - win_w/2`）
- **启动位置统一**：软件启动时先将隐藏主窗口移动到与托盘右键「设置」相同的默认位置
- **工作区感知**：窗口定位使用 `GetMonitorInfoW` 获取显示器工作区域（排除任务栏），确保窗口不会被任务栏遮挡
- **主线程安全**：快捷键回调中的窗口操作统一派发到主线程执行（`run_on_main_thread`），避免跨线程窗口操作导致的崩溃
- **原生窗口定位**：托盘设置入口使用 Windows `SetWindowPos` API 辅助定位，并在 `show()` 前后各调用一次确保位置生效
- **普通唤起保留位置**：快捷键和托盘左键唤起不再每次重置到默认位置，保留用户拖动后的当前窗口位置
- **改名数据迁移**：从 `SuperClipboard3` 升级到 `SuperClipboard` 时自动迁移旧数据目录中的 `clipboard.db`

### 修复

- **快捷键注册竞态**：修改 `set_shortcut` 为先注册新快捷键再注销旧快捷键，防止注册失败时丢失快捷键
- **快捷键录制**：支持 Windows 键组合，录制期间临时禁用全局唤醒/隐藏；再次点击录制按钮可取消，相同快捷键重新保存也会重新生效
- **窗口位置重置**：修复普通唤起窗口时每次被重置到默认位置的问题；现在只有托盘右键菜单「设置」会重置窗口位置
- **备忘录暗黑模式**：备忘录条目样式从硬编码颜色改为 CSS 变量，暗黑模式下正确适配
- **重复托盘图标**：增加单实例启动，反复点击快捷图标时唤起已有窗口，不再创建新的托盘实例

### 清理

- 移除未使用的 `storageSize` 翻译键和 `getCursorPosition` 前端 API
- 移除隐藏的保存位置/跟随模式运行时分支，`window_position` 模块仅保留设置入口默认定位与边界修正
- 新安装首次创建数据库时写入默认设置，已有用户升级时不覆盖已保存设置

### 技术细节

- 新增 `WindowPositionService::default_position()`：计算主屏右半区域中心并对工作区边界进行修正
- 新增 `set_window_position_native()` 辅助函数：封装 `SetWindowPos` 调用，设置入口 show 前后各定位一次
- Cargo.toml 保留 `Win32_Graphics_Gdi` feature（`GetMonitorInfoW`、`MonitorFromPoint` 所需）

## v1.5.0 (2026-06-23)

### 新功能

- **剪贴板内容编辑**：点击条目上的编辑按钮可修改剪贴板内容，编辑后显示「最后编辑时间」，原内容折叠保留，可通过下拉按钮展开查看
- **原格式预览开关**：设置面板新增「原格式预览」选项，开启后以等宽字体完整显示剪贴板原始内容
- **自动检查更新**：设置面板新增「自动检查更新」开关，开启后每次启动应用自动检查新版本，版本号显示在设置面板右上角

- **备忘录功能**：新增独立备忘录模块，与剪贴板分开存储（SQLite `memos` 表）
  - 支持标题、正文、标签
  - 支持搜索、置顶、删除
  - 每次按键实时保存到数据库，切换条目自动加载最新数据
  - 可在设置中开启/关闭备忘录入口
  - 备忘录标签位于"全部"左侧，使用紫色主题

- **窗口置顶开关**：设置面板新增"窗口置顶"选项，默认开启
  - 切换后立即生效，设置持久化到 SQLite

- **数据库大小显示**：底栏实时显示数据库文件占用空间

### 改进

- **默认快捷键改为 Shift+V**：更简短的唤起快捷键（可在设置中自定义）
- **标签栏滚轮滑动**：Tab 栏支持鼠标滚轮水平滚动，应对标签过多溢出的场景
- **剪贴板分类器重构**：
  - 从单一正则匹配改为评分制，多信号加权判断
  - 新增 CSS 检测：属性声明（`property: value;`）、选择器块（`.class { ... }`）
  - 新增 HTML/XML 标签检测
  - 新增 SQL 关键词检测（SELECT / INSERT / JOIN 等）
  - 新增 Shell 命令检测（`sudo`, `npm`, `docker`, `git` 等）
  - 扩充代码关键词覆盖：Rust、Go、Python、JS/TS、PHP、Java 等
  - 支持单行代码片段识别（不再要求必须多行）
  - 自然语言文本不会被误判为代码

### 修复

- **UTF-8 截断崩溃**：修复中文等多字节字符在生成预览时因字节截断导致 panic（改用 `chars().take(n)`）
- **备忘录切换数据不刷新**：点击其他备忘录条目后，编辑器始终从数据库重新加载最新数据，而非使用内存中的旧值
